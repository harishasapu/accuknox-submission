apiVersion: apps/v1
kind: Deployment
metadata:
  name: wisecow-deployment
  labels:
    app: wisecow
spec:
  replicas: 4
  revisionHistoryLimit: 0   # delete old ReplicaSets immediately
  selector:
    matchLabels:
      app: wisecow
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: wisecow
    spec:
      containers:
      - name: wisecow
        image: harishasapu/wisecow:bdc35e1fe5aa7cc1a34c1ac1cfe05f1052a4f271
        ports:
        - containerPort: 4499
